# Code generated by skv2. DO NOT EDIT.

{{- $painter := $.Values.painter}}
---

{{- define "painter.deploymentSpec"}}

# Deployment manifest for painter

apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: painter
    deployment: labels
  annotations:
    app.kubernetes.io/name: painter
    deployment: annotation
  name: painter
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    matchLabels:
      app: painter
      pod: labels
  template:
    metadata:
      labels:
        app: painter
        pod: labels
      annotations:
        app.kubernetes.io/name: painter
        prometheus.io/path: /metrics
        prometheus.io/port: "9091"
        prometheus.io/scrape: "true"
        pod: annotations
    spec:
      serviceAccountName: painter
      volumes:
      - emptyDir: {}
        name: paint
      containers:
{{- $painterContainer := $.Values.painter }}
{{- $painterImage := $painterContainer.image }}
      - name: painter
        image: {{ $painterImage.registry }}/{{ $painterImage.repository }}:{{ $painterImage.tag }}
        imagePullPolicy: {{ $painterImage.pullPolicy }}
        args:
        - foo
{{- if $painterContainer.env }}
        env:
{{ toYaml $painterContainer.env | indent 10 }}
{{- end }}
        resources:
{{- if $painterContainer.resources }}
{{ toYaml $painterContainer.resources | indent 10}}
{{- else}}
          requests:
            cpu: 500m
            memory: 256Mi
{{- end}}
        securityContext:
          runAsNonRoot: true
          runAsUser: 10101
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        readinessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 5
          periodSeconds: 10
{{- $paletteContainer := $.Values.painter.sidecars.palette }}
{{- $paletteImage := $paletteContainer.image }}
      - name: palette
        image: {{ $paletteImage.registry }}/{{ $paletteImage.repository }}:{{ $paletteImage.tag }}
        imagePullPolicy: {{ $paletteImage.pullPolicy }}
        args:
        - bar
        - baz
{{- if $paletteContainer.env }}
        env:
{{ toYaml $paletteContainer.env | indent 10 }}
{{- end }}
        resources:
{{- if $paletteContainer.resources }}
{{ toYaml $paletteContainer.resources | indent 10}}
{{- else}}
          requests:
            cpu: 500m
            memory: 256Mi
{{- end}}
        securityContext:
          runAsNonRoot: true
          runAsUser: 10101
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        livenessProbe:
          httpGet:
            path: /
            port: 8080
          initialDelaySeconds: 30
          periodSeconds: 60
      {{- if $painterImage.pullSecret }}
      imagePullSecrets:
        - name: {{ $painterImage.pullSecret }}
      {{- end}}

{{- end }} {{/* define "painter.deploymentSpec" */}}

{{/* Render painter deployment template with overrides from values*/}}
{{- $painterDeploymentOverrides := dict }}
{{- if $painter.deploymentOverrides }}
{{- $painterDeploymentOverrides = $painter.deploymentOverrides  }}
{{- end }}
---
{{ include "skv2.utils.merge" (list . $painterDeploymentOverrides "painter.deploymentSpec") }}

---

apiVersion: v1
kind: ServiceAccount
metadata:
  labels:
    app: painter
  name: painter
  namespace: {{ $.Release.Namespace }}


{{- define "painter.serviceSpec"}}


{{- end }} {{/* define "painter.serviceSpec" */}}

{{/* Render painter service template with overrides from values*/}}
{{- $painterServiceOverrides := dict }}
{{- if $painter.serviceOverrides }}
{{- $painterServiceOverrides = $painter.serviceOverrides  }}
{{- end }}
---
{{ include "skv2.utils.merge" (list . $painterServiceOverrides "painter.serviceSpec") }}


