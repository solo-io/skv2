# Code generated by skv2. DO NOT EDIT.


# Rbac manifests for painter

{{- $painter := $.Values.painter }}
{{ if $painter.enabled }}


---

kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: painter-{{ default .Release.Namespace $painter.namespace }}
  labels:
    app: painter
rules:
- verbs:
  - GET

---

kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: painter-{{ default .Release.Namespace $painter.namespace }}
  labels:
    app: painter
subjects:
- kind: ServiceAccount
  name: painter
  namespace: {{ default .Release.Namespace $painter.namespace }}
roleRef:
  kind: ClusterRole
  name: painter-{{ default .Release.Namespace $painter.namespace }}
  apiGroup: rbac.authorization.k8s.io

{{- $painterNsToResources := dict }}
{{- $painterNamespacedResources := dict }}
{{- range $entry := $painter.namespacedRbac }}
  {{- range $ns := $entry.namespaces }}
    {{- set $painterNsToResources $ns (concat $entry.resources (get $painterNsToResources $ns | default list) | mustUniq) }}
  {{- end }}
  {{- range $resource := $entry.resources }}
    {{- $_ := set $painterNamespacedResources $resource true }}
  {{- end }}
{{- end }}

{{- if not (eq (len $painterNamespacedResources) 1) }}
---

kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: painter
  labels:
    app: painter
rules:
{{- if not (get $painterNamespacedResources secrets) }}
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - GET
  - LIST
  - WATCH
{{- end }}

---

kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: painter
  labels:
    app: painter
subjects:
- kind: ServiceAccount
  name: painter
  namespace: {{ default .Release.Namespace $painter.namespace }}
roleRef:
  kind: ClusterRole
  name: painter
  apiGroup: rbac.authorization.k8s.io
{{- end }}
  {{- range $ns, $resources := $painterNsToResources }}

---

kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: painter
  namespace: {{ $ns }}
  labels:
    app: painter
rules:
{{- if (has secrets $resources) }}
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - GET
  - LIST
  - WATCH
{{- end }}

---

kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: painter
  namespace: {{ $ns }}
  labels:
    app: painter
subjects:
- kind: ServiceAccount
  name: painter
  namespace: {{ default $.Release.Namespace $painter.namespace }}
roleRef:
  kind: Role
  name: painter
  apiGroup: rbac.authorization.k8s.io

  {{- end }}
{{- end }}