# Code generated by skv2. DO NOT EDIT.


# Rbac manifests for painter

{{- $painter := $.Values.painter }}
{{ if $painter.enabled }}

---

kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: painter-{{ default .Release.Namespace $painter.namespace }}
  labels:
    app: painter
rules:
- verbs:
  - GET
---

kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: painter-{{ default .Release.Namespace $painter.namespace }}
  labels:
    app: painter
subjects:
- kind: ServiceAccount
  name: painter
  namespace: {{ default .Release.Namespace $painter.namespace }}
roleRef:
  kind: ClusterRole
  name: painter-{{ default .Release.Namespace $painter.namespace }}
  apiGroup: rbac.authorization.k8s.io

{{- $clusterScoped := true }}
{{- if $painter.watchNamespaces }}
  {{- if len $painter.watchNamespaces }}
    {{- $clusterScoped = false }}
  {{- end }}
{{- end }}

{{- if $clusterScoped }}
---

kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: painter
  labels:
    app: painter
rules:
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - GET
  - LIST
  - WATCH
---

kind: ClusterRoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: painter
  labels:
    app: painter
subjects:
- kind: ServiceAccount
  name: painter
  namespace: {{ default .Release.Namespace $painter.namespace }}
roleRef:
  kind: ClusterRole
  name: painter
  apiGroup: rbac.authorization.k8s.io

{{- else }}
{{- $namespaces := (append $painter.watchNamespaces (default .Release.Namespace $painter.namespace)) }}
{{- range $ns := $namespaces }}
---

kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: painter
  namespace: {{ $ns }}
  labels:
    app: painter
rules:
- apiGroups:
  - ""
  resources:
  - secrets
  verbs:
  - GET
  - LIST
  - WATCH

---

kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: painter
  namespace: {{ $ns }}
  labels:
    app: painter
subjects:
- kind: ServiceAccount
  name: painter
  namespace: {{ default $.Release.Namespace $painter.namespace }}
roleRef:
  kind: Role
  name: painter
  apiGroup: rbac.authorization.k8s.io

{{- end }}
{{- end }}
{{- end }}