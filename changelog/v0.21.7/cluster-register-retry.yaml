changelog:
  - type: FIX
    description: Adds a retry mechanism to the `ReconcileSecret` function.  Previously, if a cluster manager failed to start (for whatever reason) it would both a) fail b) break compute with dirty data.  This resolves both such problems.
    issueLink: https://github.com/solo-io/gloo/issues/6081
    resolvesIssue: false 